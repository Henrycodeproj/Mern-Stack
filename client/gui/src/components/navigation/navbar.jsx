import logo from '../../images/logo.png'

export const Navbar = () =>{

    return(
        <nav>
            <img src ={logo}></img>
            <ul className="list-container">
            </ul>
        </nav>
    )
}